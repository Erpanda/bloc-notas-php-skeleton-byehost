# ==========================================
# Habilitar Rewrite
# ==========================================
RewriteEngine On

# ==========================================
# Página por defecto
# ==========================================
DirectoryIndex index.php index.html

# ==========================================
# Redirigir .php → URL limpia (SOLO FRONTEND)
# ==========================================
RewriteCond %{REQUEST_URI} !^/bloc-c/api/
RewriteCond %{THE_REQUEST} \s/+(.+?)\.php[\s?]
RewriteRule ^ /bloc-c/%1 [R=301,L]

# ==========================================
# Quitar .php internamente (VISTAS)
# ==========================================
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_URI} !^/bloc-c/api/
RewriteRule ^(.+)$ views/$1.php [L]

# ==========================================
# Seguridad
# ==========================================
Options -Indexes

# Bloquear archivos sensibles
<FilesMatch "\.(env|log|sql|md|git|gitignore|json|lock)$">
    Require all denied
</FilesMatch>

# Bloquear carpetas sensibles
RewriteRule ^(config|utils|includes|vendor)/ - [F,L]

# Headers de seguridad
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>

# ==========================================
# Compresión (gzip)
# ==========================================
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml
    AddOutputFilterByType DEFLATE text/css application/javascript application/json
</IfModule>

# ==========================================
# Caché (optimizar assets)
# ==========================================
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# ==========================================
# HTTPS (activar en producción)
# ==========================================
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
